import { createSelector } from "reselect";

// const devices = (state) => state.devices.allDevices;
// const isFail = (state) => state.devices.fail;

// export const getDevicesOnPage = createSelector(
//   [ devices, isFail, activePage ],
//   (devices, isFail) => newDevicesOnPage = devices.slice(start, end)
// )
